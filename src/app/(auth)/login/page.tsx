export default function LoginPage() {
  return (
    <main className="flex flex-1 items-center justify-center p-6">
      <div className="w-full max-w-sm space-y-4">
        <h1 className="text-2xl font-semibold">Sandra University</h1>
        <p className="text-muted-foreground text-sm">
          Sign in to continue. If you received an invite email, use the link there instead.
        </p>
        <p className="text-muted-foreground text-xs italic">Login form comes in Phase 1 tests.</p>
      </div>
    </main>
  );
}
